using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NoteCollision {

    //Handles note collision
    void OnCollisionEnter(Collision col)
    {
        //if (col.gameObject.name == "EXAMPLE")
        //{
            //Destroy(col.gameObject);
        //}
    }
}
